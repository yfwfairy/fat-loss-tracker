# 构建减脂打卡工具 (日漫田园/柔和马里奥风)

根据您的偏好，我们将摒弃原先的硬核暗黑系统，转向一款拥有**“动森/星露谷/柔和版马里奥”**氛围的治愈系打卡工具。

## User Review Required

> [!NOTE]
> *由于当前 AI 图像生成服务器负载过高，暂时无法为您立刻渲染新的效果图。但我已将文字版的设计蓝图为您准备好。*

### 全新视觉与交互设定

1. **色彩基调 (Color Palette)**
   - 我们将采用**高明度、低饱和度**的马卡龙/治样色系。
   - **背景**：奶油白或淡米色 (`#FAFAFA`, `#FCF7E8`) 替代纯白，保护眼睛且带有纸质书护眼感。
   - **点缀色**：马里奥蓝/天空蓝 (`#8BA3E8`)、草地绿/薄荷绿 (`#A8E6CF`)、原木色 (`#D0B8AC`)。

2. **Dashboard (游戏化主页)**
   - **进度条重做**：不再是“打全息怪物”，而是改成一个**“经典的横版过关进度条”**（类似超级马里奥的关卡路线）或者**“浇灌植物”**。你创造的热量缺口越多，你的小人就离终点的小旗子/城堡越近。
   - **UI 质感**：所有卡片采用“软圆角 (Soft rounded)”，像云朵一样，取消锋利的实线边框，采用极浅的同色系弥散阴影，点击时会有“Q弹”的物理动画效果。按钮的风格可以做成类似 Switch 游戏 UI 的童趣感。
   
3. **用户信息与设置入口 (User Profile Center)**
   - 将侧边栏最上方的网站 Logo 区域替换为用户的**头像与 ID（昵称）**。
   - 点击该区域，会召唤出一个精美的**弹窗/侧滑面板**（User Settings Modal）。
   - 设置面板中支持以下设置：
        - 更改头像 (暂时以挑选预设头像或随机渐变色模拟)。
        - 更改 ID/昵称。
        - 身体数据：年龄 (Age)、身高 (Height)、体重 (Weight)。
   - 界面风格保持马卡龙色系的输入框圆角、选中边框的高亮。

4. **Journal (手账日记本)**
   - 背景可以垫一层极淡的格子纹理或牛皮纸纹理。
   - 饮食照片和运动记录将以**“拍立得相纸 (Polaroid)”**挂在时间线上。
   - 字体会搭配带有手写感或圆润感的无衬线字体（如 Nunito 或 Varela Round）。

4. **自定义日期选择器 (仿 Arco Design)**
   - 弃用系统原生 `input[type="date"]`。
   - 实现两个悬浮面板面板组件 (Popover)：
        - **年月抛面板**：用于在月视图下，快速下拉选择某年某月。
        - **精准日历面板**：用于在日视图下，弹出一个带前后切月按钮的网格状小日历。
   - 颜色和选中态全部使用 CSS 变量，完美融入马卡龙配色村庄风，拒绝组件库默认样式割裂感。

如果您确认这个“治愈/童趣/柔和游戏风”的文字构思符合您的想象，我们就可以跳过效果图，直接开始编写代码，在浏览器里把这个精美的界面用 CSS 敲出来！

## Proposed Changes

考虑到要实现上述这种细腻的、手绘/游戏感风格，除了结构外，更重要的是 CSS 样式的功底。我们依然保持使用原生前端三剑客的极速组合：

#### [MODIFY] [index.html](file:///Users/yang/.gemini/antigravity/scratch/fat-loss-tracker/index.html)
- 建立主 HTML 骨架。
- 引入治愈系 Google Fonts 字体（如 `Nunito` 或 `Mali`）。
   
#### [MODIFY] [style.css](file:///Users/yang/.gemini/antigravity/scratch/fat-loss-tracker/css/style.css)
- 写入全局 Soft UI (柔和拟物/新拟态) 风格。
- 添加 Q 弹的过渡动画 `cubic-bezier(0.175, 0.885, 0.32, 1.275)`。

#### [MODIFY] `js/views/...`
- 生成带有可爱 Icon 的 DOM 结构。 

## Verification Plan

- 浏览器直开验证。
- 确保页面在切换时带有柔软顺滑的过渡，各个组件视觉上呈现不伤眼的低饱和度色盘。
