Arco Design 样式的核心：
1. 日期部分：
- 它有一个悬浮弹出框 (Popover)
- 顶部可以切 年、月
- 下方是一个网格的日期表，悬停会有淡蓝色框，选中会有主题色实心背景
- 包含非当前月日期的灰显

2. 月份选择部分：
- 网格展示 1~12 月
- 选完直接收起

因为我们要保持“田园柔和风”：
- 悬浮面板背景色保持纯白或 `--bg-surface`，利用 `--shadow-soft` 加阴影。
- 选中的颜色不是 Arco 默认蓝，是 `--color-primary` (抹茶绿) 或 `--color-accent` (西柚红)。
- 字体圆润。
